# ArcPredict · AI Lens 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给 ArcPredict 增加"用户主动触发的 AI 公允概率透镜"（DeepSeek 原生 API + 服务端共享缓存 + 零合约侵入），覆盖加密二元市场和世界杯多结果事件市场。

**Architecture:** Next 14 Route Handler 串起 5 个职责单一的 lib 模块（schema / cache / budget / prompts / llm + 2 类 context builder）；前端 4 个 React 组件（DriftChip / Gauge / Compact / Panel）；DeepSeek 用原生 fetch 直调，无额外 SDK 依赖；新增 zod 一个依赖做输入输出 schema 强校验。

**Tech Stack:** Next.js 14 (App Router) · React 18 · TypeScript · Tailwind 3.4 · zod · Vitest · DeepSeek REST API（fetch 原生调用）

**Source spec:** `docs/superpowers/specs/2026-06-15-ai-lens-design.md`

---

## 工作目录与命令约定

所有命令默认在 `web/` 下运行：

```bash
cd web
pnpm install                  # 增加 zod 后必跑
pnpm typecheck                # tsc --noEmit
pnpm lint                     # next lint
pnpm vitest run test/lens.*   # 跑 AI Lens 相关单测
pnpm build                    # 最后 phase 跑一次
pnpm dev                      # 目视验证
```

环境变量（`.env.local`）：

```ini
DEEPSEEK_API_KEY=sk-...
DEEPSEEK_MODEL=deepseek-v4
DEEPSEEK_BASE_URL=https://api.deepseek.com
LENS_DAILY_BUDGET_USD=1.0
LENS_CRYPTO_TTL_HOURS=6
LENS_EVENT_TTL_HOURS=24
LENS_PRICE_DRIFT_THRESHOLD=0.05
LENS_INPUT_PRICE_PER_M_TOKENS=0.07
LENS_OUTPUT_PRICE_PER_M_TOKENS=1.10
```

每个 Task 完成后单独 commit；commit 信息中文，`feat(lens): …` / `test(lens): …` / `chore(lens): …` 前缀。

---

## 文件结构总览

新增：

```
web/lib/lens/
  schema.ts                 # Zod 输入输出契约（含 binary / multi 判别）
  prompts.ts                # 系统提示词 + user message 模板
  cache.ts                  # 进程内 Map + 文件 dump，inputHash 计算
  budget.ts                 # 日预算累计 + UTC 重置
  llm.ts                    # DeepSeek 原生 fetch 客户端（含流式）
  contextBuilders/
    crypto.ts               # 从 Pyth recent prices 构造上下文
    event.ts                # 从 worldcup-facts.json 读事实
  index.ts                  # 公共导出
web/data/
  worldcup-facts.json       # 静态事实表（admin 维护）
web/app/api/lens/
  [marketId]/
    route.ts                # POST 触发 / GET 读缓存
web/components/
  AILensDriftChip.tsx       # rich / cheap / aligned 标签
  AILensGauge.tsx           # 二元水平条 + 多结果堆叠条形
  AILensCompact.tsx         # 市场卡底部 Lens 条
  AILensPanel.tsx           # 详情页完整面板
web/test/
  lens.schema.test.ts
  lens.cache.test.ts
  lens.prompts.test.ts
  lens.budget.test.ts
  lens.context-crypto.test.ts
  lens.context-event.test.ts
  lens.llm.test.ts
  lens.route.test.ts
  lens.compact.test.tsx
  lens.panel.test.tsx
cache/
  lens.json                 # 进程退出 dump，git 忽略
```

修改：

```
web/package.json              # 增加 zod 依赖
web/.gitignore                # 增加 cache/lens.json
web/.env.example              # 增加 LENS_* 与 DEEPSEEK_* 占位
web/components/BaseMarketCard.tsx     # slot: 接入 <AILensCompact>
web/components/CryptoMarketCard.tsx   # 通过 BaseMarketCard 自动获得
web/components/WorldCupMarketCard.tsx # slot: 同上
web/app/market/[id]/page.tsx          # 接入 <AILensPanel>（确认实际路径）
```

---

## Task 0: 准备工作

**Files:**
- Modify: `web/package.json`
- Modify: `web/.gitignore`
- Modify: `web/.env.example`

- [ ] **Step 1: 加 zod 依赖**

Run:
```bash
cd web && pnpm add zod
```

Expected: `package.json` `dependencies` 出现 `"zod": "^3.x.x"`，`pnpm-lock.yaml` 更新。

- [ ] **Step 2: 更新 .gitignore**

把 `cache/lens.json` 加入忽略：

```bash
cd web
echo "" >> .gitignore
echo "# AI Lens local cache dump" >> .gitignore
echo "../cache/lens.json" >> .gitignore
```

（注意：`cache/` 目录在仓库根级，`web/.gitignore` 里写相对仓库根的路径不生效；改为编辑仓库根 `.gitignore`）

实际操作：编辑仓库根 `.gitignore`，追加：

```
# AI Lens local cache
cache/lens.json
```

- [ ] **Step 3: 在 .env.example 追加占位**

```bash
cd web && cat <<'EOF' >> .env.example

# AI Lens（DeepSeek）
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4
DEEPSEEK_BASE_URL=https://api.deepseek.com
LENS_DAILY_BUDGET_USD=1.0
LENS_CRYPTO_TTL_HOURS=6
LENS_EVENT_TTL_HOURS=24
LENS_PRICE_DRIFT_THRESHOLD=0.05
LENS_INPUT_PRICE_PER_M_TOKENS=0.07
LENS_OUTPUT_PRICE_PER_M_TOKENS=1.10
EOF
```

- [ ] **Step 4: typecheck 确认无回归**

Run: `cd web && pnpm typecheck`
Expected: PASS

- [ ] **Step 5: commit**

```bash
git add web/package.json web/pnpm-lock.yaml web/.env.example .gitignore
git -c commit.gpgsign=false commit -m "chore(lens): 准备 AI Lens 依赖、env 占位与 .gitignore"
```

---

## Task 1: Zod schema（输入 + 输出 + 判别）

**Files:**
- Create: `web/lib/lens/schema.ts`
- Test: `web/test/lens.schema.test.ts`

- [ ] **Step 1: 写 test**

`web/test/lens.schema.test.ts`：

```ts
import { describe, expect, test } from 'vitest';

import {
  LensInputSchema,
  LensOutputSchema,
  FORBIDDEN_WORDS,
} from '../lib/lens/schema';

describe('lens.schema', () => {
  test('LensInput 接受合法 crypto-binary 输入', () => {
    const ok = LensInputSchema.safeParse({
      market: {
        id: 'btc-100k',
        question: 'BTC 在 2026-12-31 前是否 ≥ $100k？',
        type: 'crypto-binary',
        end_time: 1893456000,
        implied_probability: 0.32,
      },
      context: {
        pyth_recent: [{ ts: 1, price: 65000 }],
        volatility_30d: 0.42,
        distance_to_threshold_sigma: 1.8,
      },
      generated_at: 1718524800,
    });
    expect(ok.success).toBe(true);
  });

  test('LensInput 拒绝越界 implied_probability', () => {
    const bad = LensInputSchema.safeParse({
      market: {
        id: 'x', question: 'q', type: 'crypto-binary',
        end_time: 1, implied_probability: 1.5,
      },
      context: {},
      generated_at: 1,
    });
    expect(bad.success).toBe(false);
  });

  test('LensOutput binary：fair_range 必须按 [low, high] 升序', () => {
    const ok = LensOutputSchema.safeParse({
      summary: 'BTC 距阈值 1.8σ，AI 估算偏低于市场。',
      factors: ['波动率 42%', '距离阈值 1.8σ', '历史样本偏空'],
      fair_range: [0.18, 0.25],
      confidence: 'med',
      reasoning: '基于 Pyth 30 天波动率与剩余时间。',
      sources: [{ name: 'Pyth', ref: 'BTC/USD', ts: 1 }],
      caveats: ['不含突发新闻'],
    });
    expect(ok.success).toBe(true);

    const bad = LensOutputSchema.safeParse({
      summary: 'x', factors: ['a', 'b', 'c'],
      fair_range: [0.5, 0.2],
      confidence: 'low', reasoning: 'r',
      sources: [], caveats: [],
    });
    expect(bad.success).toBe(false);
  });

  test('LensOutput multi：outcome_fair_probabilities 必填', () => {
    const ok = LensOutputSchema.safeParse({
      summary: '阿根廷概率领先。',
      factors: ['近期战绩', '主力健康', '历史对阵'],
      outcome_fair_probabilities: {
        ARG: [0.45, 0.55],
        DRAW: [0.15, 0.25],
        BRA: [0.25, 0.35],
      },
      confidence: 'med',
      reasoning: 'r',
      sources: [{ name: 'facts', ref: 'arg-vs-bra', ts: 1 }],
      caveats: [],
    });
    expect(ok.success).toBe(true);
  });

  test('禁止词命中导致 schema 拒绝', () => {
    expect(FORBIDDEN_WORDS).toContain('建议下注');
    const bad = LensOutputSchema.safeParse({
      summary: '建议下注 Yes',
      factors: ['a', 'b', 'c'],
      fair_range: [0.3, 0.4],
      confidence: 'low', reasoning: 'r',
      sources: [], caveats: [],
    });
    expect(bad.success).toBe(false);
  });
});
```

- [ ] **Step 2: 跑 test 确认 fail**

Run: `cd web && pnpm vitest run test/lens.schema.test.ts`
Expected: FAIL with `Cannot find module '../lib/lens/schema'`

- [ ] **Step 3: 写实现**

`web/lib/lens/schema.ts`：

```ts
import { z } from 'zod';

export const FORBIDDEN_WORDS = [
  '建议下注', '建议买入', '建议卖出', '建议加仓',
  '推荐下注', '推荐买入', '推荐卖出',
  'recommend buy', 'recommend sell', 'recommend bet',
  'all in', 'all-in',
] as const;

const containsForbidden = (text: string): boolean =>
  FORBIDDEN_WORDS.some((w) => text.toLowerCase().includes(w.toLowerCase()));

const safeText = (max: number) =>
  z.string().max(max).refine((s) => !containsForbidden(s), {
    message: '含禁止建议性措辞',
  });

const probability = z.number().min(0).max(1);
const probRange = z
  .tuple([probability, probability])
  .refine(([lo, hi]) => lo <= hi, { message: 'fair_range 必须按 [low, high] 升序' });

const MarketMetaBinary = z.object({
  id: z.string(),
  question: z.string(),
  type: z.literal('crypto-binary'),
  end_time: z.number().int().positive(),
  implied_probability: probability,
});

const MarketMetaMulti = z.object({
  id: z.string(),
  question: z.string(),
  type: z.literal('event-multi'),
  end_time: z.number().int().positive(),
  implied_probability: probability,
  outcome_options: z.array(z.string()).min(2),
});

const CryptoContext = z.object({
  pyth_recent: z.array(z.object({ ts: z.number(), price: z.number() })).optional(),
  volatility_30d: z.number().optional(),
  distance_to_threshold_sigma: z.number().optional(),
});

const EventContext = z.object({
  facts: z
    .array(z.object({ key: z.string(), value: z.string(), source: z.string() }))
    .optional(),
});

export const LensInputSchema = z.object({
  market: z.discriminatedUnion('type', [MarketMetaBinary, MarketMetaMulti]),
  context: CryptoContext.merge(EventContext),
  generated_at: z.number().int().positive(),
});

export type LensInput = z.infer<typeof LensInputSchema>;

const SourceItem = z.object({
  name: z.string(),
  ref: z.string(),
  ts: z.number(),
});

const BaseOutputFields = {
  summary: safeText(280),
  factors: z.array(safeText(120)).min(3).max(5),
  confidence: z.enum(['low', 'med', 'high']),
  reasoning: safeText(800),
  sources: z.array(SourceItem),
  caveats: z.array(z.string().max(200)).max(3),
};

const BinaryOutput = z.object({
  ...BaseOutputFields,
  fair_range: probRange,
  outcome_fair_probabilities: z.undefined().optional(),
});

const MultiOutput = z.object({
  ...BaseOutputFields,
  fair_range: z.undefined().optional(),
  outcome_fair_probabilities: z.record(z.string(), probRange).refine(
    (rec) => Object.keys(rec).length >= 2,
    { message: 'outcome_fair_probabilities 至少 2 项' },
  ),
});

export const LensOutputSchema = z.union([BinaryOutput, MultiOutput]);
export type LensOutput = z.infer<typeof LensOutputSchema>;
```

- [ ] **Step 4: 跑 test 确认 pass**

Run: `cd web && pnpm vitest run test/lens.schema.test.ts`
Expected: 5 tests PASS

- [ ] **Step 5: typecheck + commit**

```bash
cd web && pnpm typecheck
cd ..
git add web/lib/lens/schema.ts web/test/lens.schema.test.ts
git -c commit.gpgsign=false commit -m "feat(lens): Zod schema + 输入输出契约 + 禁止词校验"
```

---

## Task 2: Prompts 模板

**Files:**
- Create: `web/lib/lens/prompts.ts`
- Test: `web/test/lens.prompts.test.ts`

- [ ] **Step 1: 写 test**

`web/test/lens.prompts.test.ts`：

```ts
import { describe, expect, test } from 'vitest';

import { buildSystemPrompt, buildUserMessage } from '../lib/lens/prompts';
import type { LensInput } from '../lib/lens/schema';

const cryptoInput: LensInput = {
  market: {
    id: 'btc-100k',
    question: 'BTC 在 2026-12-31 前是否 ≥ $100k？',
    type: 'crypto-binary',
    end_time: 1893456000,
    implied_probability: 0.32,
  },
  context: {
    pyth_recent: [{ ts: 1, price: 65000 }],
    volatility_30d: 0.42,
    distance_to_threshold_sigma: 1.8,
  },
  generated_at: 1718524800,
};

describe('lens.prompts', () => {
  test('system prompt 包含五条铁律关键短语', () => {
    const sp = buildSystemPrompt();
    expect(sp).toMatch(/只输出.*JSON/);
    expect(sp).toMatch(/禁止.*建议下注/);
    expect(sp).toMatch(/\[unverified\]/);
    expect(sp).toMatch(/high.*≥\s*2/);
    expect(sp).toMatch(/fair_range.*<\s*5pp/);
  });

  test('user message 是合法 JSON 字符串且包含 market.id', () => {
    const msg = buildUserMessage(cryptoInput);
    expect(() => JSON.parse(msg)).not.toThrow();
    const obj = JSON.parse(msg);
    expect(obj.market.id).toBe('btc-100k');
  });
});
```

- [ ] **Step 2: 跑 test 确认 fail**

Run: `cd web && pnpm vitest run test/lens.prompts.test.ts`
Expected: FAIL with `Cannot find module '../lib/lens/prompts'`

- [ ] **Step 3: 写实现**

`web/lib/lens/prompts.ts`：

```ts
import type { LensInput } from './schema';

export function buildSystemPrompt(): string {
  return `你是 ArcPredict 的"公允概率参考机"。你的唯一职责是根据用户提供的 context 输出符合 schema 的 JSON。

## 五条铁律

1. **只输出 JSON**：不允许任何对话性前缀、解释、Markdown 围栏；返回内容必须能被 JSON.parse 解析。
2. **禁止建议性措辞**：summary / factors / reasoning 中不得出现"建议下注 / 建议买入 / 建议卖出 / 建议加仓 / All-in"或其英文同义词；只描述事实与概率推理。
3. **未验证事实必须标注 [unverified]**：任何不在 context_bundle 里的"事实"必须在 reasoning 中用 \`[unverified]\` 前缀。
4. **confidence 升级条件**：'high' 仅当来自 sources 的印证 ≥ 2 个；单一来源最多 'med'。
5. **fair_range 宽度 < 5pp 强制降级**：如果 binary 市场的 fair_range[high] - fair_range[low] < 0.05，confidence 必须 ≤ 'med'（防过度自信）。

## 输出 schema

- summary: string ≤ 280 字符
- factors: 3–5 项 string，每项 ≤ 120 字符
- 对于 crypto-binary：fair_range: [low, high]，均 ∈ [0,1]，low ≤ high
- 对于 event-multi：outcome_fair_probabilities: { [outcome]: [low, high] }，至少 2 项
- confidence: 'low' | 'med' | 'high'
- reasoning: string ≤ 800 字符
- sources: [{ name, ref, ts }]
- caveats: ≤ 3 项 string

`;
}

export function buildUserMessage(input: LensInput): string {
  return JSON.stringify(input);
}
```

- [ ] **Step 4: 跑 test 确认 pass**

Run: `cd web && pnpm vitest run test/lens.prompts.test.ts`
Expected: 2 tests PASS

- [ ] **Step 5: typecheck + commit**

```bash
cd web && pnpm typecheck
cd ..
git add web/lib/lens/prompts.ts web/test/lens.prompts.test.ts
git -c commit.gpgsign=false commit -m "feat(lens): 系统提示词 + user message 构造"
```

---

## Task 3: Cache 层（内存 + 文件 dump）

**Files:**
- Create: `web/lib/lens/cache.ts`
- Test: `web/test/lens.cache.test.ts`

- [ ] **Step 1: 写 test**

`web/test/lens.cache.test.ts`：

```ts
import { afterEach, beforeEach, describe, expect, test } from 'vitest';

import { createMemoryCache, computeInputHash } from '../lib/lens/cache';
import type { LensInput, LensOutput } from '../lib/lens/schema';

const baseInput: LensInput = {
  market: {
    id: 'm1', question: 'q', type: 'crypto-binary',
    end_time: 1, implied_probability: 0.3,
  },
  context: { pyth_recent: [{ ts: 1, price: 100 }] },
  generated_at: 1,
};

const sampleOutput: LensOutput = {
  summary: 's', factors: ['a', 'b', 'c'],
  fair_range: [0.1, 0.2], confidence: 'low',
  reasoning: 'r', sources: [], caveats: [],
};

describe('lens.cache', () => {
  let cache: ReturnType<typeof createMemoryCache>;

  beforeEach(() => {
    cache = createMemoryCache({ nowMs: () => 1_000_000 });
  });

  test('miss 时返回 undefined', () => {
    const hash = computeInputHash(baseInput);
    expect(cache.get('m1', hash)).toBeUndefined();
  });

  test('set 后 get 命中', () => {
    const hash = computeInputHash(baseInput);
    cache.set('m1', hash, sampleOutput, 6 * 60 * 60 * 1000);
    const hit = cache.get('m1', hash);
    expect(hit?.output).toEqual(sampleOutput);
  });

  test('TTL 过期后 miss', () => {
    const hash = computeInputHash(baseInput);
    cache.set('m1', hash, sampleOutput, 1000);
    cache = createMemoryCache({ nowMs: () => 1_005_000, seed: cache.dump() });
    expect(cache.get('m1', hash)).toBeUndefined();
  });

  test('不同 inputHash 视为不同条目', () => {
    const otherInput = { ...baseInput, context: { pyth_recent: [{ ts: 2, price: 999 }] } };
    expect(computeInputHash(baseInput)).not.toBe(computeInputHash(otherInput));
  });

  test('同样输入哈希稳定', () => {
    expect(computeInputHash(baseInput)).toBe(computeInputHash(baseInput));
  });
});
```

- [ ] **Step 2: 跑 test 确认 fail**

Run: `cd web && pnpm vitest run test/lens.cache.test.ts`
Expected: FAIL

- [ ] **Step 3: 写实现**

`web/lib/lens/cache.ts`：

```ts
import { createHash } from 'node:crypto';
import type { LensInput, LensOutput } from './schema';

export type CacheEntry = {
  output: LensOutput;
  storedAtMs: number;
  expiresAtMs: number;
};

export type CacheDump = Record<string, CacheEntry>;

export type CacheOptions = {
  nowMs?: () => number;
  seed?: CacheDump;
};

export function computeInputHash(input: LensInput): string {
  const canonical = JSON.stringify(input, Object.keys(input).sort());
  return createHash('sha1').update(canonical).digest('hex').slice(0, 12);
}

const key = (marketId: string, inputHash: string) => `${marketId}:${inputHash}`;

export function createMemoryCache(opts: CacheOptions = {}) {
  const now = opts.nowMs ?? (() => Date.now());
  const store: Map<string, CacheEntry> = new Map();
  if (opts.seed) {
    for (const [k, v] of Object.entries(opts.seed)) {
      if (v.expiresAtMs > now()) store.set(k, v);
    }
  }

  return {
    get(marketId: string, inputHash: string): CacheEntry | undefined {
      const entry = store.get(key(marketId, inputHash));
      if (!entry) return undefined;
      if (entry.expiresAtMs <= now()) {
        store.delete(key(marketId, inputHash));
        return undefined;
      }
      return entry;
    },

    set(marketId: string, inputHash: string, output: LensOutput, ttlMs: number) {
      const t = now();
      store.set(key(marketId, inputHash), {
        output,
        storedAtMs: t,
        expiresAtMs: t + ttlMs,
      });
    },

    dump(): CacheDump {
      const out: CacheDump = {};
      for (const [k, v] of store.entries()) out[k] = v;
      return out;
    },
  };
}
```

- [ ] **Step 4: 跑 test 确认 pass**

Run: `cd web && pnpm vitest run test/lens.cache.test.ts`
Expected: 5 tests PASS

- [ ] **Step 5: typecheck + commit**

```bash
cd web && pnpm typecheck
cd ..
git add web/lib/lens/cache.ts web/test/lens.cache.test.ts
git -c commit.gpgsign=false commit -m "feat(lens): 内存缓存 + inputHash 计算"
```

---

## Task 4: 日预算追踪

**Files:**
- Create: `web/lib/lens/budget.ts`
- Test: `web/test/lens.budget.test.ts`

- [ ] **Step 1: 写 test**

`web/test/lens.budget.test.ts`：

```ts
import { describe, expect, test } from 'vitest';

import { createBudgetTracker } from '../lib/lens/budget';

describe('lens.budget', () => {
  test('未超额时 allow=true，并累积 spent', () => {
    const b = createBudgetTracker({
      dailyLimitUsd: 1.0,
      nowMs: () => Date.UTC(2026, 5, 16, 10, 0, 0),
    });
    expect(b.canSpend(0.0005)).toBe(true);
    b.record(0.0005);
    expect(b.spentToday()).toBeCloseTo(0.0005);
  });

  test('达上限拒绝继续', () => {
    const b = createBudgetTracker({
      dailyLimitUsd: 0.001,
      nowMs: () => Date.UTC(2026, 5, 16, 10, 0, 0),
    });
    b.record(0.0009);
    expect(b.canSpend(0.0002)).toBe(false);
  });

  test('跨过 UTC 0 点自动重置', () => {
    let now = Date.UTC(2026, 5, 16, 23, 59, 0);
    const b = createBudgetTracker({
      dailyLimitUsd: 0.001,
      nowMs: () => now,
    });
    b.record(0.001);
    expect(b.canSpend(0.0005)).toBe(false);
    now = Date.UTC(2026, 5, 17, 0, 1, 0);
    expect(b.canSpend(0.0005)).toBe(true);
    expect(b.spentToday()).toBe(0);
  });

  test('estimateCostUsd 按 token 数与单价计算', () => {
    const b = createBudgetTracker({
      dailyLimitUsd: 1,
      inputPricePerMTokens: 0.07,
      outputPricePerMTokens: 1.10,
      nowMs: () => 0,
    });
    const cost = b.estimateCostUsd(1000, 1000);
    // input: 1000 * 0.07 / 1e6 = 0.00007
    // output: 1000 * 1.10 / 1e6 = 0.0011
    expect(cost).toBeCloseTo(0.00117);
  });
});
```

- [ ] **Step 2: 跑 test 确认 fail**

Run: `cd web && pnpm vitest run test/lens.budget.test.ts`
Expected: FAIL

- [ ] **Step 3: 写实现**

`web/lib/lens/budget.ts`：

```ts
export type BudgetOptions = {
  dailyLimitUsd: number;
  inputPricePerMTokens?: number;
  outputPricePerMTokens?: number;
  nowMs?: () => number;
};

const dayKey = (ms: number): string => {
  const d = new Date(ms);
  return `${d.getUTCFullYear()}-${d.getUTCMonth()}-${d.getUTCDate()}`;
};

export function createBudgetTracker(opts: BudgetOptions) {
  const now = opts.nowMs ?? (() => Date.now());
  const inputPrice = opts.inputPricePerMTokens ?? 0.07;
  const outputPrice = opts.outputPricePerMTokens ?? 1.10;
  let currentDay = dayKey(now());
  let spent = 0;

  const rollover = () => {
    const today = dayKey(now());
    if (today !== currentDay) {
      currentDay = today;
      spent = 0;
    }
  };

  return {
    canSpend(usd: number): boolean {
      rollover();
      return spent + usd <= opts.dailyLimitUsd;
    },
    record(usd: number) {
      rollover();
      spent += usd;
    },
    spentToday(): number {
      rollover();
      return spent;
    },
    estimateCostUsd(inputTokens: number, outputTokens: number): number {
      return (inputTokens * inputPrice + outputTokens * outputPrice) / 1_000_000;
    },
  };
}
```

- [ ] **Step 4: 跑 test 确认 pass**

Run: `cd web && pnpm vitest run test/lens.budget.test.ts`
Expected: 4 tests PASS

- [ ] **Step 5: typecheck + commit**

```bash
cd web && pnpm typecheck
cd ..
git add web/lib/lens/budget.ts web/test/lens.budget.test.ts
git -c commit.gpgsign=false commit -m "feat(lens): 日预算追踪 + UTC 重置 + 成本估算"
```

---

## Task 5: Crypto context builder

**Files:**
- Create: `web/lib/lens/contextBuilders/crypto.ts`
- Test: `web/test/lens.context-crypto.test.ts`

- [ ] **Step 1: 写 test**

`web/test/lens.context-crypto.test.ts`：

```ts
import { describe, expect, test } from 'vitest';

import { buildCryptoContext } from '../lib/lens/contextBuilders/crypto';

const samples = Array.from({ length: 7 * 24 }, (_, i) => ({
  ts: 1_700_000_000 + i * 3600,
  price: 60000 + Math.sin(i / 5) * 2000,
}));

describe('lens.contextBuilders.crypto', () => {
  test('从 ts/price 序列计算 volatility 与 sigma 距离', () => {
    const ctx = buildCryptoContext({
      pythSeries: samples,
      threshold: 70000,
    });
    expect(ctx.pyth_recent?.length).toBeGreaterThan(0);
    expect(ctx.volatility_30d).toBeGreaterThan(0);
    expect(ctx.distance_to_threshold_sigma).toBeGreaterThan(0);
  });

  test('空序列降级返回空字段', () => {
    const ctx = buildCryptoContext({ pythSeries: [], threshold: 70000 });
    expect(ctx.pyth_recent).toEqual([]);
    expect(ctx.volatility_30d).toBeUndefined();
    expect(ctx.distance_to_threshold_sigma).toBeUndefined();
  });

  test('采样不超过 maxSamples', () => {
    const ctx = buildCryptoContext({
      pythSeries: samples,
      threshold: 70000,
      maxSamples: 24,
    });
    expect(ctx.pyth_recent!.length).toBeLessThanOrEqual(24);
  });
});
```

- [ ] **Step 2: 跑 test 确认 fail**

Run: `cd web && pnpm vitest run test/lens.context-crypto.test.ts`
Expected: FAIL

- [ ] **Step 3: 写实现**

`web/lib/lens/contextBuilders/crypto.ts`：

```ts
export type PythSample = { ts: number; price: number };

export type CryptoContextInput = {
  pythSeries: PythSample[];
  threshold: number;
  maxSamples?: number;
};

export type CryptoContext = {
  pyth_recent: PythSample[];
  volatility_30d?: number;
  distance_to_threshold_sigma?: number;
};

export function buildCryptoContext(input: CryptoContextInput): CryptoContext {
  const { pythSeries, threshold, maxSamples = 168 } = input;
  if (pythSeries.length === 0) {
    return { pyth_recent: [] };
  }
  const stride = Math.max(1, Math.floor(pythSeries.length / maxSamples));
  const sampled = pythSeries.filter((_, i) => i % stride === 0).slice(0, maxSamples);

  const prices = pythSeries.map((p) => p.price);
  const mean = prices.reduce((a, b) => a + b, 0) / prices.length;
  const variance = prices.reduce((acc, p) => acc + (p - mean) ** 2, 0) / prices.length;
  const stdev = Math.sqrt(variance);
  const volatility = mean === 0 ? 0 : stdev / mean;
  const latest = prices[prices.length - 1];
  const distance = stdev === 0 ? 0 : (threshold - latest) / stdev;

  return {
    pyth_recent: sampled,
    volatility_30d: volatility,
    distance_to_threshold_sigma: distance,
  };
}
```

- [ ] **Step 4: 跑 test 确认 pass**

Run: `cd web && pnpm vitest run test/lens.context-crypto.test.ts`
Expected: 3 tests PASS

- [ ] **Step 5: typecheck + commit**

```bash
cd web && pnpm typecheck
cd ..
git add web/lib/lens/contextBuilders/crypto.ts web/test/lens.context-crypto.test.ts
git -c commit.gpgsign=false commit -m "feat(lens): crypto context builder（采样+波动率+sigma 距离）"
```

---

## Task 6: Event context builder + 世界杯事实表种子

**Files:**
- Create: `web/data/worldcup-facts.json`
- Create: `web/lib/lens/contextBuilders/event.ts`
- Test: `web/test/lens.context-event.test.ts`

- [ ] **Step 1: 创建初始事实表种子**

`web/data/worldcup-facts.json`：

```json
{
  "version": 1,
  "updated_at": "2026-06-16",
  "facts": {
    "global": [
      { "key": "tournament", "value": "2026 FIFA World Cup", "source": "FIFA" },
      { "key": "hosts", "value": "USA / Canada / Mexico", "source": "FIFA" },
      { "key": "format", "value": "48 队 / 12 小组 / 32 强淘汰", "source": "FIFA" }
    ],
    "by_team": {
      "ARG": [
        { "key": "form", "value": "卫冕冠军，南美预选第 1", "source": "CONMEBOL" }
      ],
      "BRA": [
        { "key": "form", "value": "南美预选第 2，主力健康", "source": "CONMEBOL" }
      ]
    },
    "by_match": {}
  }
}
```

- [ ] **Step 2: 写 test**

`web/test/lens.context-event.test.ts`：

```ts
import { describe, expect, test } from 'vitest';

import { buildEventContext } from '../lib/lens/contextBuilders/event';
import factsTable from '../data/worldcup-facts.json';

describe('lens.contextBuilders.event', () => {
  test('返回 global facts', () => {
    const ctx = buildEventContext({
      factsTable: factsTable as any,
      matchId: null,
      teams: [],
    });
    expect(ctx.facts?.some((f) => f.key === 'tournament')).toBe(true);
  });

  test('给定 teams 返回 by_team 条目', () => {
    const ctx = buildEventContext({
      factsTable: factsTable as any,
      matchId: null,
      teams: ['ARG', 'BRA'],
    });
    const teamKeys = ctx.facts?.map((f) => f.source) ?? [];
    expect(teamKeys.some((s) => s === 'CONMEBOL')).toBe(true);
  });

  test('未知 team 静默跳过，不抛错', () => {
    expect(() =>
      buildEventContext({
        factsTable: factsTable as any,
        matchId: null,
        teams: ['XYZ'],
      }),
    ).not.toThrow();
  });
});
```

- [ ] **Step 3: 跑 test 确认 fail**

Run: `cd web && pnpm vitest run test/lens.context-event.test.ts`
Expected: FAIL

- [ ] **Step 4: 写实现**

`web/lib/lens/contextBuilders/event.ts`：

```ts
export type Fact = { key: string; value: string; source: string };

export type FactsTable = {
  version: number;
  updated_at: string;
  facts: {
    global: Fact[];
    by_team: Record<string, Fact[]>;
    by_match: Record<string, Fact[]>;
  };
};

export type EventContextInput = {
  factsTable: FactsTable;
  matchId: string | null;
  teams: string[];
};

export type EventContext = {
  facts: Fact[];
};

export function buildEventContext(input: EventContextInput): EventContext {
  const { factsTable, matchId, teams } = input;
  const out: Fact[] = [];
  out.push(...factsTable.facts.global);
  for (const t of teams) {
    const items = factsTable.facts.by_team[t];
    if (items) out.push(...items);
  }
  if (matchId) {
    const items = factsTable.facts.by_match[matchId];
    if (items) out.push(...items);
  }
  return { facts: out };
}
```

- [ ] **Step 5: 跑 test 确认 pass**

Run: `cd web && pnpm vitest run test/lens.context-event.test.ts`
Expected: 3 tests PASS

- [ ] **Step 6: typecheck + commit**

```bash
cd web && pnpm typecheck
cd ..
git add web/lib/lens/contextBuilders/event.ts web/test/lens.context-event.test.ts web/data/worldcup-facts.json
git -c commit.gpgsign=false commit -m "feat(lens): event context builder + 世界杯事实表种子"
```

---

## Task 7: DeepSeek 原生 fetch 客户端

**Files:**
- Create: `web/lib/lens/llm.ts`
- Test: `web/test/lens.llm.test.ts`

- [ ] **Step 1: 写 test**

`web/test/lens.llm.test.ts`：

```ts
import { afterEach, describe, expect, test, vi } from 'vitest';

import { callDeepSeek, type DeepSeekConfig } from '../lib/lens/llm';

const baseConfig: DeepSeekConfig = {
  apiKey: 'sk-test',
  baseUrl: 'https://api.deepseek.test',
  model: 'deepseek-v4',
  timeoutMs: 200,
  maxRetries: 1,
};

afterEach(() => {
  vi.restoreAllMocks();
});

describe('lens.llm.callDeepSeek', () => {
  test('成功响应返回 parsed content', async () => {
    const fetchMock = vi.fn().mockResolvedValue(
      new Response(
        JSON.stringify({
          choices: [{ message: { content: JSON.stringify({ ok: true }) } }],
          usage: { prompt_tokens: 100, completion_tokens: 50 },
        }),
        { status: 200, headers: { 'content-type': 'application/json' } },
      ),
    );
    const result = await callDeepSeek({
      config: baseConfig,
      systemPrompt: 'sys',
      userMessage: 'usr',
      fetchImpl: fetchMock as any,
    });
    expect(result.contentJson).toEqual({ ok: true });
    expect(result.usage.promptTokens).toBe(100);
    expect(result.usage.completionTokens).toBe(50);
    expect(fetchMock).toHaveBeenCalledTimes(1);
    const call = fetchMock.mock.calls[0];
    expect(call[0]).toContain('/chat/completions');
  });

  test('网络失败重试 1 次', async () => {
    const fetchMock = vi
      .fn()
      .mockRejectedValueOnce(new Error('boom'))
      .mockResolvedValueOnce(
        new Response(
          JSON.stringify({
            choices: [{ message: { content: '{}' } }],
            usage: { prompt_tokens: 0, completion_tokens: 0 },
          }),
          { status: 200 },
        ),
      );
    const result = await callDeepSeek({
      config: baseConfig,
      systemPrompt: 'sys',
      userMessage: 'usr',
      fetchImpl: fetchMock as any,
    });
    expect(result.contentJson).toEqual({});
    expect(fetchMock).toHaveBeenCalledTimes(2);
  });

  test('非 JSON content 抛错', async () => {
    const fetchMock = vi.fn().mockResolvedValue(
      new Response(
        JSON.stringify({
          choices: [{ message: { content: 'not json at all' } }],
          usage: { prompt_tokens: 0, completion_tokens: 0 },
        }),
        { status: 200 },
      ),
    );
    await expect(
      callDeepSeek({
        config: baseConfig,
        systemPrompt: 'sys',
        userMessage: 'usr',
        fetchImpl: fetchMock as any,
      }),
    ).rejects.toThrow(/JSON/);
  });
});
```

- [ ] **Step 2: 跑 test 确认 fail**

Run: `cd web && pnpm vitest run test/lens.llm.test.ts`
Expected: FAIL

- [ ] **Step 3: 写实现**

`web/lib/lens/llm.ts`：

```ts
export type DeepSeekConfig = {
  apiKey: string;
  baseUrl: string;
  model: string;
  timeoutMs: number;
  maxRetries: number;
};

export type DeepSeekCallParams = {
  config: DeepSeekConfig;
  systemPrompt: string;
  userMessage: string;
  fetchImpl?: typeof fetch;
};

export type DeepSeekResult = {
  contentJson: unknown;
  usage: { promptTokens: number; completionTokens: number };
};

const sleep = (ms: number) => new Promise((r) => setTimeout(r, ms));

export async function callDeepSeek(params: DeepSeekCallParams): Promise<DeepSeekResult> {
  const { config, systemPrompt, userMessage } = params;
  const fetchImpl = params.fetchImpl ?? fetch;

  const body = {
    model: config.model,
    messages: [
      { role: 'system', content: systemPrompt },
      { role: 'user', content: userMessage },
    ],
    response_format: { type: 'json_object' },
    temperature: 0.2,
  };

  let lastErr: unknown = null;
  for (let attempt = 0; attempt <= config.maxRetries; attempt++) {
    const controller = new AbortController();
    const timer = setTimeout(() => controller.abort(), config.timeoutMs);
    try {
      const res = await fetchImpl(`${config.baseUrl}/chat/completions`, {
        method: 'POST',
        headers: {
          'Content-Type': 'application/json',
          Authorization: `Bearer ${config.apiKey}`,
        },
        body: JSON.stringify(body),
        signal: controller.signal,
      });
      clearTimeout(timer);
      if (!res.ok) throw new Error(`DeepSeek HTTP ${res.status}`);
      const payload = (await res.json()) as {
        choices: { message: { content: string } }[];
        usage: { prompt_tokens: number; completion_tokens: number };
      };
      const content = payload.choices?.[0]?.message?.content ?? '';
      let parsed: unknown;
      try {
        parsed = JSON.parse(content);
      } catch (e) {
        throw new Error('DeepSeek 返回非 JSON 内容');
      }
      return {
        contentJson: parsed,
        usage: {
          promptTokens: payload.usage?.prompt_tokens ?? 0,
          completionTokens: payload.usage?.completion_tokens ?? 0,
        },
      };
    } catch (err) {
      clearTimeout(timer);
      lastErr = err;
      if (attempt < config.maxRetries) {
        await sleep(500 * (attempt + 1));
        continue;
      }
    }
  }
  throw lastErr ?? new Error('DeepSeek 调用失败');
}
```

- [ ] **Step 4: 跑 test 确认 pass**

Run: `cd web && pnpm vitest run test/lens.llm.test.ts`
Expected: 3 tests PASS

- [ ] **Step 5: typecheck + commit**

```bash
cd web && pnpm typecheck
cd ..
git add web/lib/lens/llm.ts web/test/lens.llm.test.ts
git -c commit.gpgsign=false commit -m "feat(lens): DeepSeek 原生 fetch 客户端（超时 + 重试 + JSON 解析）"
```

---

## Task 8: Route Handler `/api/lens/[marketId]`

**Files:**
- Create: `web/lib/lens/index.ts`
- Create: `web/app/api/lens/[marketId]/route.ts`
- Test: `web/test/lens.route.test.ts`

- [ ] **Step 1: 写 lib/lens/index.ts 公共导出**

`web/lib/lens/index.ts`：

```ts
export * from './schema';
export * from './prompts';
export * from './cache';
export * from './budget';
export * from './llm';
export * from './contextBuilders/crypto';
export * from './contextBuilders/event';

export type LensRouteResponseOk = {
  status: 'ok';
  cached: boolean;
  output: import('./schema').LensOutput;
  meta: { last_updated_ms: number; input_hash: string };
};

export type LensRouteResponseErr = {
  status: 'error';
  code:
    | 'invalid_market'
    | 'budget_exhausted'
    | 'llm_failure'
    | 'schema_failure'
    | 'rate_limited';
  message: string;
};

export type LensRouteResponse = LensRouteResponseOk | LensRouteResponseErr;
```

- [ ] **Step 2: 写 test**

`web/test/lens.route.test.ts`：

```ts
import { afterEach, describe, expect, test, vi } from 'vitest';

import { handleLensRequest } from '../app/api/lens/[marketId]/route';
import type { LensInput } from '../lib/lens/schema';

const fakeInput: LensInput = {
  market: {
    id: 'm1', question: 'q', type: 'crypto-binary',
    end_time: 9_999_999_999, implied_probability: 0.3,
  },
  context: { pyth_recent: [{ ts: 1, price: 100 }] },
  generated_at: 1718524800,
};

const fakeLensOutput = {
  summary: 's', factors: ['a', 'b', 'c'],
  fair_range: [0.1, 0.2], confidence: 'low',
  reasoning: 'r', sources: [], caveats: [],
};

afterEach(() => vi.restoreAllMocks());

describe('lens.route.handleLensRequest', () => {
  test('miss 时调用 LLM 并写缓存', async () => {
    const callLLM = vi.fn().mockResolvedValue({
      contentJson: fakeLensOutput,
      usage: { promptTokens: 100, completionTokens: 80 },
    });
    const result = await handleLensRequest({
      input: fakeInput,
      cache: undefined,
      callLLM,
      budget: { canSpend: () => true, record: () => {}, estimateCostUsd: () => 0.0003 },
      ttlMs: 60_000,
    });
    expect(result.status).toBe('ok');
    expect(callLLM).toHaveBeenCalledTimes(1);
  });

  test('cache hit 不调用 LLM', async () => {
    const callLLM = vi.fn();
    const result = await handleLensRequest({
      input: fakeInput,
      cache: {
        output: fakeLensOutput as any,
        storedAtMs: 0,
        expiresAtMs: 9_999_999_999_999,
      },
      callLLM,
      budget: { canSpend: () => true, record: () => {}, estimateCostUsd: () => 0 },
      ttlMs: 60_000,
    });
    expect(result.status).toBe('ok');
    if (result.status === 'ok') expect(result.cached).toBe(true);
    expect(callLLM).not.toHaveBeenCalled();
  });

  test('预算耗尽返回 budget_exhausted', async () => {
    const callLLM = vi.fn();
    const result = await handleLensRequest({
      input: fakeInput,
      cache: undefined,
      callLLM,
      budget: { canSpend: () => false, record: () => {}, estimateCostUsd: () => 0.0003 },
      ttlMs: 60_000,
    });
    expect(result.status).toBe('error');
    if (result.status === 'error') expect(result.code).toBe('budget_exhausted');
  });

  test('LLM 输出 schema 不合法返回 schema_failure', async () => {
    const callLLM = vi.fn().mockResolvedValue({
      contentJson: { foo: 'bar' },
      usage: { promptTokens: 100, completionTokens: 80 },
    });
    const result = await handleLensRequest({
      input: fakeInput,
      cache: undefined,
      callLLM,
      budget: { canSpend: () => true, record: () => {}, estimateCostUsd: () => 0.0003 },
      ttlMs: 60_000,
    });
    expect(result.status).toBe('error');
    if (result.status === 'error') expect(result.code).toBe('schema_failure');
  });
});
```

- [ ] **Step 3: 跑 test 确认 fail**

Run: `cd web && pnpm vitest run test/lens.route.test.ts`
Expected: FAIL

- [ ] **Step 4: 写实现**

`web/app/api/lens/[marketId]/route.ts`：

```ts
import { NextRequest, NextResponse } from 'next/server';

import {
  LensInputSchema,
  LensOutputSchema,
  computeInputHash,
  createMemoryCache,
  createBudgetTracker,
  buildSystemPrompt,
  buildUserMessage,
  callDeepSeek,
  type LensInput,
  type LensOutput,
  type LensRouteResponse,
  type CacheEntry,
} from '@/lib/lens';

// 模块级单例（dev 热更可能多份；prod 单 worker 单份）
const cache = createMemoryCache();
const budget = createBudgetTracker({
  dailyLimitUsd: Number(process.env.LENS_DAILY_BUDGET_USD ?? 1.0),
  inputPricePerMTokens: Number(process.env.LENS_INPUT_PRICE_PER_M_TOKENS ?? 0.07),
  outputPricePerMTokens: Number(process.env.LENS_OUTPUT_PRICE_PER_M_TOKENS ?? 1.10),
});

export type HandleLensParams = {
  input: LensInput;
  cache: CacheEntry | undefined;
  callLLM: (args: { systemPrompt: string; userMessage: string }) => Promise<{
    contentJson: unknown;
    usage: { promptTokens: number; completionTokens: number };
  }>;
  budget: {
    canSpend: (usd: number) => boolean;
    record: (usd: number) => void;
    estimateCostUsd: (inputTokens: number, outputTokens: number) => number;
  };
  ttlMs: number;
  nowMs?: () => number;
};

export async function handleLensRequest(params: HandleLensParams): Promise<
  LensRouteResponse & { _newOutput?: { output: LensOutput; inputHash: string; ttlMs: number } }
> {
  const { input, cache: hit, callLLM, budget: b, ttlMs } = params;
  const now = params.nowMs ?? (() => Date.now());
  const inputHash = computeInputHash(input);

  if (hit) {
    return {
      status: 'ok',
      cached: true,
      output: hit.output,
      meta: { last_updated_ms: hit.storedAtMs, input_hash: inputHash },
    };
  }

  const estCost = b.estimateCostUsd(600, 300);
  if (!b.canSpend(estCost)) {
    return { status: 'error', code: 'budget_exhausted', message: '日预算已用尽' };
  }

  let raw: { contentJson: unknown; usage: { promptTokens: number; completionTokens: number } };
  try {
    raw = await callLLM({
      systemPrompt: buildSystemPrompt(),
      userMessage: buildUserMessage(input),
    });
  } catch (e) {
    return { status: 'error', code: 'llm_failure', message: String(e) };
  }

  const actualCost = b.estimateCostUsd(raw.usage.promptTokens, raw.usage.completionTokens);
  b.record(actualCost);

  const parsed = LensOutputSchema.safeParse(raw.contentJson);
  if (!parsed.success) {
    return { status: 'error', code: 'schema_failure', message: parsed.error.message };
  }

  return {
    status: 'ok',
    cached: false,
    output: parsed.data,
    meta: { last_updated_ms: now(), input_hash: inputHash },
    _newOutput: { output: parsed.data, inputHash, ttlMs },
  };
}

export async function POST(req: NextRequest, { params }: { params: { marketId: string } }) {
  let body: unknown;
  try {
    body = await req.json();
  } catch {
    return NextResponse.json(
      { status: 'error', code: 'invalid_market', message: '请求体不是 JSON' },
      { status: 400 },
    );
  }
  const parsedInput = LensInputSchema.safeParse(body);
  if (!parsedInput.success) {
    return NextResponse.json(
      { status: 'error', code: 'invalid_market', message: parsedInput.error.message },
      { status: 400 },
    );
  }
  const input = parsedInput.data;
  if (input.market.id !== params.marketId) {
    return NextResponse.json(
      { status: 'error', code: 'invalid_market', message: 'marketId 不匹配' },
      { status: 400 },
    );
  }

  const inputHash = computeInputHash(input);
  const hit = cache.get(input.market.id, inputHash);
  const ttlMs =
    input.market.type === 'crypto-binary'
      ? Number(process.env.LENS_CRYPTO_TTL_HOURS ?? 6) * 60 * 60 * 1000
      : Number(process.env.LENS_EVENT_TTL_HOURS ?? 24) * 60 * 60 * 1000;

  const apiKey = process.env.DEEPSEEK_API_KEY ?? '';
  const baseUrl = process.env.DEEPSEEK_BASE_URL ?? 'https://api.deepseek.com';
  const model = process.env.DEEPSEEK_MODEL ?? 'deepseek-v4';

  const result = await handleLensRequest({
    input,
    cache: hit,
    callLLM: async ({ systemPrompt, userMessage }) =>
      callDeepSeek({
        config: { apiKey, baseUrl, model, timeoutMs: 5000, maxRetries: 1 },
        systemPrompt,
        userMessage,
      }),
    budget,
    ttlMs,
  });

  if (result.status === 'ok' && '_newOutput' in result && result._newOutput) {
    cache.set(input.market.id, result._newOutput.inputHash, result._newOutput.output, result._newOutput.ttlMs);
    delete (result as Record<string, unknown>)._newOutput;
  }

  const httpStatus =
    result.status === 'ok'
      ? 200
      : result.code === 'budget_exhausted'
      ? 429
      : result.code === 'invalid_market'
      ? 400
      : 502;
  return NextResponse.json(result, { status: httpStatus });
}

export const dynamic = 'force-dynamic';
```

- [ ] **Step 5: 跑 test 确认 pass**

Run: `cd web && pnpm vitest run test/lens.route.test.ts`
Expected: 4 tests PASS

- [ ] **Step 6: typecheck + commit**

```bash
cd web && pnpm typecheck
cd ..
git add web/lib/lens/index.ts web/app/api/lens/[marketId]/route.ts web/test/lens.route.test.ts
git -c commit.gpgsign=false commit -m "feat(lens): /api/lens/[marketId] route handler + handleLensRequest 纯函数"
```

---

## Task 9: AILensDriftChip 组件

**Files:**
- Create: `web/components/AILensDriftChip.tsx`

- [ ] **Step 1: 写实现（无单测，纯视觉小组件）**

`web/components/AILensDriftChip.tsx`：

```tsx
type Props = {
  impliedProb: number;
  fairLow: number;
  fairHigh: number;
  threshold?: number;
};

export function AILensDriftChip({ impliedProb, fairLow, fairHigh, threshold = 0.05 }: Props) {
  const fairMid = (fairLow + fairHigh) / 2;
  const gap = impliedProb - fairMid;
  let label: string;
  let color: string;
  if (Math.abs(gap) < threshold) {
    label = '≈ aligned';
    color = 'text-ink-3 border-hair';
  } else if (gap > 0) {
    label = '↑ rich';
    color = 'text-heat border-heat/40';
  } else {
    label = '↓ cheap';
    color = 'text-arc-glow border-arc-glow/40';
  }
  const pct = (n: number) => `${(n * 100).toFixed(0)}%`;
  return (
    <span
      className={`inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[11px] border ${color} bg-glass backdrop-blur-sm`}
      role="status"
      aria-label={`市场 ${pct(impliedProb)}，AI ${pct(fairLow)} 到 ${pct(fairHigh)}，${label}`}
    >
      <span>市场 {pct(impliedProb)}</span>
      <span className="opacity-40">/</span>
      <span>
        AI {pct(fairLow)}–{pct(fairHigh)}
      </span>
      <span className="ml-1">{label}</span>
    </span>
  );
}
```

- [ ] **Step 2: typecheck**

Run: `cd web && pnpm typecheck`
Expected: PASS

- [ ] **Step 3: commit**

```bash
git add web/components/AILensDriftChip.tsx
git -c commit.gpgsign=false commit -m "feat(lens): AILensDriftChip 三态（rich/cheap/aligned）"
```

---

## Task 10: AILensGauge 组件（binary + multi）

**Files:**
- Create: `web/components/AILensGauge.tsx`

- [ ] **Step 1: 写实现**

`web/components/AILensGauge.tsx`：

```tsx
type BinaryProps = {
  variant: 'binary';
  impliedProb: number;
  fairLow: number;
  fairHigh: number;
};

type MultiProps = {
  variant: 'multi';
  rows: {
    outcome: string;
    impliedProb: number;
    fairLow: number;
    fairHigh: number;
  }[];
};

export function AILensGauge(props: BinaryProps | MultiProps) {
  if (props.variant === 'binary') return <BinaryBar {...props} />;
  return <MultiStack rows={props.rows} />;
}

const pct = (n: number) => `${(n * 100).toFixed(0)}%`;

function BinaryBar({ impliedProb, fairLow, fairHigh }: BinaryProps) {
  return (
    <div className="space-y-2">
      <div className="relative h-3 rounded-full bg-bg-2 overflow-hidden">
        <div
          className="absolute top-0 bottom-0 bg-arc-glow/30"
          style={{ left: `${fairLow * 100}%`, width: `${(fairHigh - fairLow) * 100}%` }}
          aria-hidden
        />
        <div
          className="absolute top-1/2 -translate-y-1/2 h-4 w-0.5 bg-ink"
          style={{ left: `${impliedProb * 100}%` }}
          aria-hidden
        />
      </div>
      <div className="flex justify-between text-xs text-ink-3">
        <span>市场 {pct(impliedProb)}</span>
        <span>AI {pct(fairLow)}–{pct(fairHigh)}</span>
      </div>
    </div>
  );
}

function MultiStack({ rows }: { rows: MultiProps['rows'] }) {
  return (
    <div className="space-y-3">
      {rows.map((row) => (
        <div key={row.outcome}>
          <div className="flex justify-between text-xs mb-1">
            <span className="text-ink-2">{row.outcome}</span>
            <span className="text-ink-3">
              市场 {pct(row.impliedProb)} · AI {pct(row.fairLow)}–{pct(row.fairHigh)}
            </span>
          </div>
          <div className="relative h-2 rounded-full bg-bg-2 overflow-hidden">
            <div
              className="absolute top-0 bottom-0 bg-arc-glow/30"
              style={{
                left: `${row.fairLow * 100}%`,
                width: `${(row.fairHigh - row.fairLow) * 100}%`,
              }}
              aria-hidden
            />
            <div
              className="absolute top-1/2 -translate-y-1/2 h-3 w-0.5 bg-ink"
              style={{ left: `${row.impliedProb * 100}%` }}
              aria-hidden
            />
          </div>
        </div>
      ))}
    </div>
  );
}
```

- [ ] **Step 2: typecheck + commit**

```bash
cd web && pnpm typecheck
cd ..
git add web/components/AILensGauge.tsx
git -c commit.gpgsign=false commit -m "feat(lens): AILensGauge（binary 水平条 + multi 堆叠条形）"
```

---

## Task 11: AILensCompact 组件（市场卡底部条）

**Files:**
- Create: `web/components/AILensCompact.tsx`
- Test: `web/test/lens.compact.test.tsx`

- [ ] **Step 1: 写 test**

`web/test/lens.compact.test.tsx`：

```tsx
// @vitest-environment jsdom
import { afterEach, beforeEach, describe, expect, test, vi } from 'vitest';
import React from 'react';
import { createRoot, type Root } from 'react-dom/client';
import { act } from 'react';

import { AILensCompact } from '../components/AILensCompact';
import type { LensInput, LensOutput } from '../lib/lens/schema';

const baseInput: LensInput = {
  market: {
    id: 'm1', question: 'q', type: 'crypto-binary',
    end_time: 9_999_999_999, implied_probability: 0.3,
  },
  context: {},
  generated_at: 1,
};

const fakeOutput: LensOutput = {
  summary: '近一周波动率上升，AI 估算偏低于市场。',
  factors: ['a', 'b', 'c'],
  fair_range: [0.1, 0.2],
  confidence: 'med',
  reasoning: 'r', sources: [], caveats: [],
};

let container: HTMLDivElement;
let root: Root;

beforeEach(() => {
  container = document.createElement('div');
  document.body.appendChild(container);
  root = createRoot(container);
});

afterEach(() => {
  act(() => root.unmount());
  container.remove();
  vi.restoreAllMocks();
});

describe('AILensCompact', () => {
  test('初始 idle：显示按钮文字', () => {
    act(() => {
      root.render(<AILensCompact input={baseInput} fetchImpl={vi.fn()} />);
    });
    expect(container.textContent).toContain('Ask AI');
  });

  test('点击触发：成功后显示 summary 与 drift chip', async () => {
    const fetchMock = vi.fn().mockResolvedValue(
      new Response(
        JSON.stringify({
          status: 'ok',
          cached: false,
          output: fakeOutput,
          meta: { last_updated_ms: 0, input_hash: 'h' },
        }),
        { status: 200 },
      ),
    );
    act(() => {
      root.render(<AILensCompact input={baseInput} fetchImpl={fetchMock as any} />);
    });
    const btn = container.querySelector('button')!;
    await act(async () => {
      btn.click();
      await Promise.resolve();
      await Promise.resolve();
    });
    expect(fetchMock).toHaveBeenCalled();
    expect(container.textContent).toContain('近一周波动率上升');
    expect(container.textContent).toContain('市场 30%');
  });
});
```

- [ ] **Step 2: 跑 test 确认 fail**

Run: `cd web && pnpm vitest run test/lens.compact.test.tsx`
Expected: FAIL

- [ ] **Step 3: 写实现**

`web/components/AILensCompact.tsx`：

```tsx
'use client';

import { useState } from 'react';

import type { LensInput, LensOutput } from '@/lib/lens/schema';
import { AILensDriftChip } from './AILensDriftChip';

type Props = {
  input: LensInput;
  fetchImpl?: typeof fetch;
};

type State =
  | { kind: 'idle' }
  | { kind: 'loading' }
  | { kind: 'result'; output: LensOutput; cached: boolean; lastUpdatedMs: number }
  | { kind: 'error'; message: string };

export function AILensCompact({ input, fetchImpl }: Props) {
  const [state, setState] = useState<State>({ kind: 'idle' });
  const f = fetchImpl ?? fetch;

  const trigger = async () => {
    setState({ kind: 'loading' });
    try {
      const res = await f(`/api/lens/${encodeURIComponent(input.market.id)}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(input),
      });
      const json = await res.json();
      if (json.status === 'ok') {
        setState({
          kind: 'result',
          output: json.output,
          cached: !!json.cached,
          lastUpdatedMs: json.meta?.last_updated_ms ?? Date.now(),
        });
      } else {
        setState({ kind: 'error', message: json.message ?? '调用失败' });
      }
    } catch (e) {
      setState({ kind: 'error', message: String(e) });
    }
  };

  if (state.kind === 'idle') {
    return (
      <div className="flex items-center justify-between gap-3 px-3 py-2 border-t border-hair text-xs">
        <button
          type="button"
          onClick={trigger}
          className="inline-flex items-center gap-1 px-2.5 py-1 rounded-full bg-glass border border-hair hover:border-arc-glow/50 transition"
          aria-label="触发 AI 概率分析"
        >
          ✨ Ask AI
        </button>
        <span className="text-ink-3">AI 概率分析 · 24h 缓存共享</span>
      </div>
    );
  }
  if (state.kind === 'loading') {
    return (
      <div className="px-3 py-2 border-t border-hair text-xs text-ink-2">
        Analyzing…
      </div>
    );
  }
  if (state.kind === 'error') {
    return (
      <div className="px-3 py-2 border-t border-hair text-xs text-no">
        AI Lens 暂不可用，请稍后重试
        <button className="ml-2 underline" onClick={trigger}>
          重试
        </button>
      </div>
    );
  }
  if (state.output.fair_range) {
    return (
      <div className="px-3 py-2 border-t border-hair flex items-center justify-between gap-3 text-xs">
        <span className="text-ink-2 truncate">{state.output.summary}</span>
        <AILensDriftChip
          impliedProb={input.market.implied_probability}
          fairLow={state.output.fair_range[0]}
          fairHigh={state.output.fair_range[1]}
        />
      </div>
    );
  }
  return (
    <div className="px-3 py-2 border-t border-hair text-xs text-ink-2">
      {state.output.summary}
    </div>
  );
}
```

- [ ] **Step 4: 跑 test 确认 pass**

Run: `cd web && pnpm vitest run test/lens.compact.test.tsx`
Expected: 2 tests PASS

- [ ] **Step 5: typecheck + commit**

```bash
cd web && pnpm typecheck
cd ..
git add web/components/AILensCompact.tsx web/test/lens.compact.test.tsx
git -c commit.gpgsign=false commit -m "feat(lens): AILensCompact（idle/loading/result/error 四态）"
```

---

## Task 12: AILensPanel 组件（详情页完整面板）

**Files:**
- Create: `web/components/AILensPanel.tsx`
- Test: `web/test/lens.panel.test.tsx`

- [ ] **Step 1: 写 test**

`web/test/lens.panel.test.tsx`：

```tsx
// @vitest-environment jsdom
import { afterEach, beforeEach, describe, expect, test, vi } from 'vitest';
import React from 'react';
import { createRoot, type Root } from 'react-dom/client';
import { act } from 'react';

import { AILensPanel } from '../components/AILensPanel';
import type { LensInput, LensOutput } from '../lib/lens/schema';

const baseInput: LensInput = {
  market: {
    id: 'm1', question: 'q', type: 'crypto-binary',
    end_time: 9_999_999_999, implied_probability: 0.3,
  },
  context: {},
  generated_at: 1,
};

const fakeOutput: LensOutput = {
  summary: 'BTC 估算偏低。',
  factors: ['波动率高', '距阈值远', '历史空头偏多'],
  fair_range: [0.1, 0.22],
  confidence: 'med',
  reasoning: '基于 Pyth 30 天波动率推理。',
  sources: [{ name: 'Pyth', ref: 'BTC/USD', ts: 1 }],
  caveats: ['不含突发新闻'],
};

let container: HTMLDivElement;
let root: Root;

beforeEach(() => {
  container = document.createElement('div');
  document.body.appendChild(container);
  root = createRoot(container);
});

afterEach(() => {
  act(() => root.unmount());
  container.remove();
  vi.restoreAllMocks();
});

describe('AILensPanel', () => {
  test('初始显示 Generate 按钮', () => {
    act(() => {
      root.render(<AILensPanel input={baseInput} fetchImpl={vi.fn()} />);
    });
    expect(container.textContent).toContain('Generate AI Lens');
    expect(container.textContent).toContain('非投顾建议');
  });

  test('成功后显示 summary / factors / sources / 免责脚标', async () => {
    const fetchMock = vi.fn().mockResolvedValue(
      new Response(
        JSON.stringify({
          status: 'ok',
          cached: false,
          output: fakeOutput,
          meta: { last_updated_ms: Date.now(), input_hash: 'h' },
        }),
        { status: 200 },
      ),
    );
    act(() => {
      root.render(<AILensPanel input={baseInput} fetchImpl={fetchMock as any} />);
    });
    const btn = container.querySelector('button')!;
    await act(async () => {
      btn.click();
      await Promise.resolve();
      await Promise.resolve();
    });
    expect(container.textContent).toContain('BTC 估算偏低');
    expect(container.textContent).toContain('波动率高');
    expect(container.textContent).toContain('Pyth · BTC/USD');
    expect(container.textContent).toContain('Not financial advice');
  });
});
```

- [ ] **Step 2: 跑 test 确认 fail**

Run: `cd web && pnpm vitest run test/lens.panel.test.tsx`
Expected: FAIL

- [ ] **Step 3: 写实现**

`web/components/AILensPanel.tsx`：

```tsx
'use client';

import { useState } from 'react';

import type { LensInput, LensOutput } from '@/lib/lens/schema';
import { AILensGauge } from './AILensGauge';

type Props = {
  input: LensInput;
  fetchImpl?: typeof fetch;
};

type State =
  | { kind: 'idle' }
  | { kind: 'loading' }
  | { kind: 'result'; output: LensOutput; cached: boolean; lastUpdatedMs: number }
  | { kind: 'error'; message: string };

const DISCLAIMER =
  'AI estimate based on listed sources. Not financial advice. Not a settlement oracle.';

export function AILensPanel({ input, fetchImpl }: Props) {
  const [state, setState] = useState<State>({ kind: 'idle' });
  const [whyOpen, setWhyOpen] = useState(false);
  const f = fetchImpl ?? fetch;

  const trigger = async () => {
    setState({ kind: 'loading' });
    try {
      const res = await f(`/api/lens/${encodeURIComponent(input.market.id)}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify(input),
      });
      const json = await res.json();
      if (json.status === 'ok') {
        setState({
          kind: 'result',
          output: json.output,
          cached: !!json.cached,
          lastUpdatedMs: json.meta?.last_updated_ms ?? Date.now(),
        });
      } else {
        setState({ kind: 'error', message: json.message ?? '调用失败' });
      }
    } catch (e) {
      setState({ kind: 'error', message: String(e) });
    }
  };

  if (state.kind === 'idle') {
    return (
      <section className="rounded-xl bg-glass border border-hair backdrop-blur-xl p-8 text-center">
        <button
          type="button"
          onClick={trigger}
          className="inline-flex items-center gap-2 px-5 py-2.5 rounded-full bg-arc/20 border border-arc-glow/40 text-ink hover:border-arc-glow transition"
        >
          ✨ Generate AI Lens
        </button>
        <p className="mt-3 text-xs text-ink-3">
          AI 综合 Pyth 价格 / 市场状态 / 事实表给出公允概率区间，仅供参考、非投顾建议
        </p>
      </section>
    );
  }

  if (state.kind === 'loading') {
    return (
      <section className="rounded-xl bg-glass border border-hair p-6">
        <p className="text-sm text-ink-2">AI 正在分析…</p>
      </section>
    );
  }

  if (state.kind === 'error') {
    return (
      <section className="rounded-xl bg-glass border border-hair p-6">
        <p className="text-sm text-no">AI Lens 暂不可用 · {state.message}</p>
        <button onClick={trigger} className="mt-2 text-xs underline text-arc-glow">
          重试
        </button>
      </section>
    );
  }

  const out = state.output;
  const isBinary = !!out.fair_range;

  return (
    <section className="rounded-xl bg-glass border border-hair backdrop-blur-xl p-6 space-y-5">
      <header className="space-y-2">
        <p className="text-sm text-ink">{out.summary}</p>
        <p className="text-[11px] text-ink-3">
          信心 {out.confidence} · {state.cached ? 'Cached' : 'Fresh'} ·{' '}
          {new Date(state.lastUpdatedMs).toLocaleString()}
        </p>
      </header>

      <div>
        {isBinary && out.fair_range && (
          <AILensGauge
            variant="binary"
            impliedProb={input.market.implied_probability}
            fairLow={out.fair_range[0]}
            fairHigh={out.fair_range[1]}
          />
        )}
        {!isBinary && out.outcome_fair_probabilities && (
          <AILensGauge
            variant="multi"
            rows={Object.entries(out.outcome_fair_probabilities).map(([outcome, range]) => ({
              outcome,
              impliedProb: input.market.implied_probability,
              fairLow: range[0],
              fairHigh: range[1],
            }))}
          />
        )}
      </div>

      <ul className="grid gap-2 sm:grid-cols-2">
        {out.factors.map((f, i) => (
          <li
            key={i}
            className="rounded-lg bg-bg-2/60 border border-hair px-3 py-2 text-xs text-ink-2"
          >
            {f}
          </li>
        ))}
      </ul>

      {out.sources.length > 0 && (
        <div className="flex flex-wrap gap-2">
          {out.sources.map((s, i) => (
            <span
              key={i}
              className="text-[11px] px-2 py-0.5 rounded-full border border-hair text-ink-3"
            >
              {s.name} · {s.ref}
            </span>
          ))}
        </div>
      )}

      <div>
        <button
          onClick={() => setWhyOpen((v) => !v)}
          className="text-xs text-arc-glow underline"
        >
          {whyOpen ? '收起 Why?' : '展开 Why?'}
        </button>
        {whyOpen && (
          <p className="mt-2 text-xs text-ink-2 leading-relaxed">{out.reasoning}</p>
        )}
      </div>

      {out.caveats.length > 0 && (
        <ul className="text-[11px] text-ink-3 list-disc list-inside">
          {out.caveats.map((c, i) => (
            <li key={i}>{c}</li>
          ))}
        </ul>
      )}

      <footer className="text-[10px] text-ink-3 border-t border-hair pt-3">
        {DISCLAIMER}
      </footer>
    </section>
  );
}
```

- [ ] **Step 4: 跑 test 确认 pass**

Run: `cd web && pnpm vitest run test/lens.panel.test.tsx`
Expected: 2 tests PASS

- [ ] **Step 5: typecheck + commit**

```bash
cd web && pnpm typecheck
cd ..
git add web/components/AILensPanel.tsx web/test/lens.panel.test.tsx
git -c commit.gpgsign=false commit -m "feat(lens): AILensPanel（gauge + factors + sources + Why? + 免责）"
```

---

## Task 13: 把 AILensCompact 接入市场卡

**Files:**
- Modify: `web/components/BaseMarketCard.tsx`
- Modify: `web/components/WorldCupMarketCard.tsx`

- [ ] **Step 1: 读现状**

Run: `cd web && head -80 components/BaseMarketCard.tsx`
Expected: 看到 card 容器的最末尾 children 渲染位置。

- [ ] **Step 2: 在 BaseMarketCard 底部追加 children slot**

如果 BaseMarketCard 已有 children slot 在 footer 之前：直接在调用方传 `<AILensCompact input={...} />`。
如果没有：在 BaseMarketCard 添加一个 `footerSlot?: React.ReactNode` prop，渲染在卡片底部紧贴边框上方。

参考改动（具体行号以实际为准）：

```tsx
type BaseMarketCardProps = {
  // ... 既有 props
  footerSlot?: React.ReactNode;
};

export function BaseMarketCard({ /* ... */, footerSlot }: BaseMarketCardProps) {
  return (
    <div className="..."> {/* 既有结构 */}
      {/* ...既有内容 */}
      {footerSlot && (
        <div className="-mx-4 sm:-mx-5 mt-3">{footerSlot}</div>
      )}
    </div>
  );
}
```

- [ ] **Step 3: 在 CryptoMarketCard / WorldCupMarketCard 调用处传 footerSlot**

```tsx
import { AILensCompact } from './AILensCompact';
import type { LensInput } from '@/lib/lens/schema';

// 在 component 内构造 lensInput
const lensInput: LensInput = {
  market: {
    id: market.id,
    question: market.question,
    type: 'crypto-binary',
    end_time: Number(market.endTime),
    implied_probability: derivedImpliedProb,
  },
  context: {
    pyth_recent: [], // v1：先传空，由后端 builder 在路由侧补充（或保留前端只发 meta，由后端拉 Pyth）
  },
  generated_at: Math.floor(Date.now() / 1000),
};

return (
  <BaseMarketCard
    // ... 既有 props
    footerSlot={<AILensCompact input={lensInput} />}
  />
);
```

对 World Cup 卡：`type: 'event-multi'`，附 `outcome_options` 数组。

> 注意：v1 实现上前端只发 `market` 元信息，`context` 由 route handler 在服务端补全更安全（避免前端拼装的不一致）。如果选这条路径，需要回到 Task 8 的 route handler 增加"前端发 meta-only，服务端补 context"的分支——本计划默认走"前端拼好完整 input"的简单路径。

- [ ] **Step 4: typecheck**

Run: `cd web && pnpm typecheck`
Expected: PASS

- [ ] **Step 5: 启动 dev 目视验证**

Run: `cd web && pnpm dev`
开浏览器 http://localhost:3000，确认：
- 首页市场卡底部出现 `✨ Ask AI` 按钮
- 点击按钮后（需要 .env.local 有真实 DEEPSEEK_API_KEY）loading→result 流程跑通
- 按钮区不破坏卡片现有布局

- [ ] **Step 6: commit**

```bash
git add web/components/BaseMarketCard.tsx web/components/WorldCupMarketCard.tsx web/components/CryptoMarketCard.tsx
git -c commit.gpgsign=false commit -m "feat(lens): 市场卡底部接入 AILensCompact slot"
```

---

## Task 14: 把 AILensPanel 接入详情页

**Files:**
- Modify: `web/app/market/[id]/page.tsx`（如果路径不同，先用 `find web/app -name 'page.tsx' | xargs grep -l MarketDetailCard` 定位）

- [ ] **Step 1: 定位详情页文件**

Run:
```bash
find web/app -name 'page.tsx' -exec grep -l 'MarketDetailCard' {} \;
```

- [ ] **Step 2: 在 MarketDetailCard 之后插入 AILensPanel**

```tsx
import { AILensPanel } from '@/components/AILensPanel';
import type { LensInput } from '@/lib/lens/schema';

// 在 component 内构造 lensInput；区分二元与多结果
const lensInput: LensInput = market.kind === 'crypto'
  ? {
      market: {
        id: market.id,
        question: market.question,
        type: 'crypto-binary',
        end_time: Number(market.endTime),
        implied_probability: derivedImpliedProb,
      },
      context: {},
      generated_at: Math.floor(Date.now() / 1000),
    }
  : {
      market: {
        id: market.id,
        question: market.question,
        type: 'event-multi',
        end_time: Number(market.endTime),
        implied_probability: derivedImpliedProb,
        outcome_options: market.outcomeOptions,
      },
      context: {},
      generated_at: Math.floor(Date.now() / 1000),
    };

// 渲染
<MarketDetailCard /* ...既有 props */ />
<AILensPanel input={lensInput} />
```

> 字段名（`market.id` / `market.kind` / `market.endTime` / `market.outcomeOptions` / `derivedImpliedProb`）请按详情页实际可用的变量名替换；命名以详情页既有数据结构为准，不引入新字段。

- [ ] **Step 3: typecheck**

Run: `cd web && pnpm typecheck`
Expected: PASS

- [ ] **Step 4: 启动 dev 目视验证**

打开任一市场详情页，确认 `Generate AI Lens` 大按钮居中渲染、点击后 panel 展开完整结构。

- [ ] **Step 5: commit**

```bash
git add web/app/market/
git -c commit.gpgsign=false commit -m "feat(lens): 详情页 MarketDetailCard 下方接入 AILensPanel"
```

---

## Task 15: build + 收尾

- [ ] **Step 1: 跑全量单测**

Run: `cd web && pnpm vitest run`
Expected: 所有 lens.* 测试 + 既有测试 PASS

- [ ] **Step 2: typecheck + lint**

```bash
cd web && pnpm typecheck && pnpm lint
```
Expected: PASS

- [ ] **Step 3: build**

Run: `cd web && pnpm build`
Expected: Next 14 build 完成，`/api/lens/[marketId]` 出现在 Route 列表里。

- [ ] **Step 4: 在 OpenSpec 下登记一份 spec 同步（如果 ops 流程要求）**

如果项目要求所有用户可见功能进 `openspec/specs/`，新建：
```
openspec/specs/ai-lens/spec.md
openspec/specs/ai-lens/design.md
```
内容参考 `docs/superpowers/specs/2026-06-15-ai-lens-design.md` 摘要。

> 如果团队约定 AI Lens 暂时只放 `docs/superpowers/`，跳过这一步。

- [ ] **Step 5: 最终 commit**

```bash
git add openspec/specs/ai-lens 2>/dev/null
git -c commit.gpgsign=false commit -m "chore(lens): build 通过 + spec 同步收尾" --allow-empty
```

---

## v1 范围裁剪声明

Spec 中以下两点**有意推迟到 v1.1**，不在本 plan 范围内：

1. **Spec §1.2 详情页 loading 态 SSE 流式打字**：v1 用普通"AI 正在分析…"占位，等基础链路稳定再加 streaming（DeepSeek 原生 SDK 已支持，前端 `EventSource` 接入即可，工作量约 1 个独立 task）。
2. **Spec §3.4 速率限制**（同 IP 60 次/小时、同 IP 同市场 5 次/小时、Regenerate 60s 冷却）：v1 仅靠 `LENS_DAILY_BUDGET_USD` 全局兜底。原因：早期用户量小 + DeepSeek 成本极低 + 共享缓存进一步压成本，全局预算已经足够防滥用。v1.1 上线时若发现 spike 再补"基于 IP 的 token bucket"。

如果实施中决定提前做其中之一，请先在 spec 末尾补一节 v1 实际范围说明，再调整本 plan。

---

## 总验收清单

- [ ] `pnpm vitest run` 所有 lens.* 测试 PASS（≥ 25 个测试用例）
- [ ] `pnpm typecheck` 0 报错
- [ ] `pnpm lint` 0 警告（或仅项目既有警告）
- [ ] `pnpm build` 成功，路由列表含 `/api/lens/[marketId]`
- [ ] 首页市场卡底部出现 `✨ Ask AI` 按钮；点击端到端跑通
- [ ] 详情页 `Generate AI Lens` 按钮点击后 panel 渲染完整
- [ ] 详情页 panel 含：summary / gauge / factors / sources / Why? / caveats / 免责脚标
- [ ] Lens 永久免责脚标在所有形态都可见
- [ ] 缓存命中第二次点击 < 100ms 返回
- [ ] 预算耗尽时按钮置灰 + 文案正确（手工把 `LENS_DAILY_BUDGET_USD=0` 验证）
- [ ] 多结果世界杯市场显示堆叠条形（手工触发一次验证）

---

*实施按 Task 顺序，禁止跳序；每个 Task 完成立即 commit；遇到 spec 没覆盖到的边界情况停下来更新 spec 而不是临时拍脑袋扩范围。*
